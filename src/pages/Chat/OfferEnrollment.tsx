import React from "react";

const OfferEnrollment: React.FC = () => {
	// get users gigs for selection in the form. cause we need to send the gig id in the enrollment creation api url

	return <div>
        <div>
            <form>
                
            </form>
        </div>
    </div>;
};

export default OfferEnrollment;
